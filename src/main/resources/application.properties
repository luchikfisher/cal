# === Application Info ===
app.name=Calculator
app.version=1.0.0

# === Behavior ===
calculator.eval.threshold=1
calculator.exit.threshold=2

# === Messages ===
calculator.exit.message=Exiting calculator.
calculator.error.empty=Nothing to evaluate.
calculator.result.prefix=Result:
calculator.prompt.input=Enter number/operator or full expression (double empty = evaluate, triple = exit):